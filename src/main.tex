\documentclass[a4paper]{book}

% Load the VUB package.
% This has many options, please read the documentation at
% https://gitlab.com/rubdos/texlive-vub
% LTeX: enabled=false
\usepackage{vub}
\usepackage{graphicx}
\usepackage{subcaption}
\usepackage{emptypage}
\usepackage[dutch, british]{babel}
\usepackage{csquotes}
\usepackage{caption}
\usepackage{float}
\usepackage{wrapfig2}
\usepackage{hyperref}
\usepackage{cleveref}
\usepackage{xcolor}
\usepackage{listings}
\usepackage[natbib,style=ieee]{biblatex}

\setlength{\parskip}{\baselineskip}
\setlength{\parindent}{0pt} % Optional: removes paragraph first-line indent


% defines the listing environment
\newfloat{listing}{htbp}{lol}
\floatname{listing}{Listing}



\addbibresource{bibliography.bib}


% Classic Light Theme Colors
\definecolor{cBackground}{rgb}{0.97, 0.97, 0.98}
\definecolor{cComment}{rgb}{0.25, 0.49, 0.19}
\definecolor{cKeyword}{rgb}{0.0, 0.35, 0.72}
\definecolor{cString}{rgb}{0.75, 0.13, 0.13}
\definecolor{cPreprocessor}{rgb}{0.55, 0.25, 0.0}
\definecolor{cLineNum}{rgb}{0.5, 0.5, 0.5}

\lstset{
    language=C,
    backgroundcolor=\color{cBackground},
    basicstyle=\ttfamily\footnotesize,
    commentstyle=\color{cComment}\itshape,
    keywordstyle=\color{cKeyword}\bfseries,
    stringstyle=\color{cString},
    numberstyle=\tiny\color{cLineNum},
    directivestyle=\color{cPreprocessor},
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=8pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=4,
    frame=single,
    framerule=0.5pt,
    rulecolor=\color{black!20},
    keywords=[2]{uint8_t,uint16_t,uint32_t,uint64_t,int8_t,int16_t,int32_t,int64_t,size_t,ssize_t,uintptr_t,NULL,bool,true,false},
    keywordstyle=[2]\color{cKeyword}\bfseries
}


\definecolor{haaiBG}{rgb}{0.97, 0.97, 0.98}
\definecolor{haaiKeyword}{rgb}{0.0, 0.35, 0.72}     % Deep Blue: def, defr, out, lambda, if
\definecolor{haaiBuiltin}{rgb}{0.55, 0.25, 0.0}     % Brown/Amber: operators (+, -, car, cons)
\definecolor{haaiComment}{rgb}{0.25, 0.49, 0.19}     % Muted Green: ; comments
\definecolor{haaiString}{rgb}{0.75, 0.13, 0.13}      % Red: "strings"
\definecolor{haaiLineNum}{rgb}{0.5, 0.5, 0.5}

% Define Haai Language
\lstdefinelanguage{Haai}{
    % Core forms and definition keywords
    keywords=[1]{defr, def, out, lambda, if, cond, let, begin, quote, when, unless, loop, emit},
    % Primitive operators & built-in functions
    keywords=[2]{+, -, *, /, =, <, >, <=, >=, and, or, not, cons, car, cdr, list, display},
    sensitive=true,
    morecomment=[l]{;},
    morestring=[b]"
}

% Global Listings Config for Haai
\lstset{
    language=Haai,
    backgroundcolor=\color{haaiBG},
    basicstyle=\ttfamily\footnotesize,
    commentstyle=\color{haaiComment}\itshape,
    keywordstyle=[1]\color{haaiKeyword}\bfseries,
    keywordstyle=[2]\color{haaiBuiltin}\bfseries,
    stringstyle=\color{haaiString},
    numberstyle=\tiny\color{haaiLineNum},
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=8pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2,
    frame=single,
    framerule=0.5pt,
    rulecolor=\color{black!20}
}

\definecolor{jskeyword}{rgb}{0.0, 0.45, 0.75}    % Blue for keywords
\definecolor{jsstring}{rgb}{0.13, 0.55, 0.13}    % Green for strings
\definecolor{jscomment}{rgb}{0.5, 0.5, 0.5}      % Gray for comments
\definecolor{jsbuiltin}{rgb}{0.65, 0.15, 0.65}    % Purple for globals/built-ins
\definecolor{jsnumber}{rgb}{0.85, 0.35, 0.0}     % Orange/Rust for numbers
\definecolor{jsbg}{rgb}{0.97, 0.97, 0.97}        % Light gray background

% Define JavaScript Language Spec (ES6+)
\lstdefinelanguage{JavaScript}{
  keywords={
    async, await, break, case, catch, class, const, continue,
    debugger, default, delete, do, else, export, extends,
    finally, for, function, if, import, in, instanceof,
    let, new, return, super, switch, this, throw, try,
    typeof, var, void, while, with, yield
  },
  morekeywords=[2]{
    % Built-in Globals & Types
    Array, Boolean, Console, Date, Error, Function, JSON, Math,
    Number, Object, Promise, RegExp, String, Symbol, Map, Set,
    console, window, document, process, undefined, null, NaN, true, false
  },
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morestring=[b]',
  morestring=[b]",
  morestring=[b]` % Template literals
}

% Configure Global Listings Style
\lstset{
  language=JavaScript,
  backgroundcolor=\color{jsbg},
  basicstyle=\ttfamily\small,
  keywordstyle=\color{jskeyword}\bfseries,
  keywordstyle=[2]\color{jsbuiltin},
  stringstyle=\color{jsstring},
  commentstyle=\color{jscomment}\itshape,
  numberstyle=\tiny\color{jscomment},
  identifierstyle=\color{black},
  %
  numbers=left,
  numbersep=8pt,
  stepnumber=1,
  breaklines=true,
  breakatwhitespace=true,
  showstringspaces=false,
  tabsize=2,
  frame=single,
  rulecolor=\color{black!15},
  captionpos=b,
  keepspaces=true,
  columns=flexible
}

% Define custom syntax colors
\definecolor{emfrpKeyword}{RGB}{0, 51, 179}      % Deep Blue
\definecolor{emfrpType}{RGB}{8, 122, 136}         % Teal
\definecolor{emfrpOperator}{RGB}{120, 0, 150}     % Purple
\definecolor{emfrpComment}{RGB}{106, 153, 85}     % Green
\definecolor{emfrpString}{RGB}{163, 21, 21}       % Dark Red
\definecolor{emfrpBackground}{RGB}{248, 249, 250} % Off-white

% Custom language definition for EmFRP
\lstdefinelanguage{EmFRP}{
  morekeywords=[1]{module, in, out, node, init, type, data, if, then, else, case, of},
  morekeywords=[2]{Int, Double, Bool, Float, Char, String}, % Built-in types
  sensitive=true,
  morecomment=[l]{--},               % Single-line comment
  morecomment=[s]{\{-}{-\}},         % Multi-line comment
  morestring=[b]",                   % Strings
  alsoletter={@},
  otherkeywords={@last, ->, =, ==, !=, &&, ||, !, +, -, *, /}
}

% Style configuration
\lstdefinestyle{EmFRPStyle}{
  language=EmFRP,
  backgroundcolor=\color{emfrpBackground},
  basicstyle=\ttfamily\small,
  keywordstyle=[1]\color{emfrpKeyword}\bfseries,
  keywordstyle=[2]\color{emfrpType}\bfseries,
  commentstyle=\color{emfrpComment}\itshape,
  stringstyle=\color{emfrpString},
  keywordstyle=\color{emfrpOperator}, % Catches otherkeywords (@last, etc.)
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  numbersep=8pt,
  showstringspaces=false,
  breaklines=true,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{black!20}
}

\colorlet{cfrpkeyword}{blue!80!black}
\colorlet{cfrptype}{teal!80!black}
\colorlet{cfrpcomment}{green!50!black}
\colorlet{cfrpstring}{orange!90!black}
\colorlet{cfrpoperator}{purple!90!black}

\lstdefinelanguage{CFRP}{
  % Class 1: Core reactivity and language keywords
  keywords=[1]{
    signal, event, stream, lift, lift2, foldp, sample,
    behavior, step, switch, hold, merge, filter, map,
    input, output, node, react, emit, when, pure, import
  },
  % Class 2: Built-in types
  keywords=[2]{Int, Float, Bool, String, Time, Signal, Event},
  sensitive=true,
  morecomment=[l]{//},
  morecomment=[s]{/*}{*/},
  morecomment=[l]{\#},
  morestring=[b]",
  morestring=[b]',
  literate=
    {<-}{{{\color{cfrpoperator}$\leftarrow$}}}2
    {->}{{{\color{cfrpoperator}$\rightarrow$}}}2
    {=>}{{{\color{cfrpoperator}$\Rightarrow$}}}2
    {<~}{{{\color{cfrpoperator}$\hookleftarrow$}}}2
    {~>}{{{\color{cfrpoperator}$\rightsquigarrow$}}}2
}

% Style / Configuration
\lstset{
  language=CFRP,
  keywordstyle=[1]\color{cfrpkeyword}\bfseries,
  keywordstyle=[2]\color{cfrptype}\bfseries,
  commentstyle=\color{cfrpcomment}\itshape,
  stringstyle=\color{cfrpstring},
  tabsize=2,
  showstringspaces=false,
  breaklines=true,
  captionpos=b
}

% LTeX: dictionary+=Haai
% LTeX: dictionary+=Gérard
% LTeX: dictionary+=Lichtert
% LTeX: dictionary+=Bjarno
% LTeX: dictionary+=Oeyen
% LTeX: dictionary+=Wolfgang
% LTeX: dictionary+=De Meuter
% LTeX: dictionary+=MCUs
% LTeX: dictionary+=Tierless
% LTeX: dictionary+=CHaai
% LTeX: dictionary+=bytecode
% LTeX: dictionary+=EmFRP
% LTeX: dictionary+=OROCOS
% LTeX: dictionary+=ROS
% LTeX: dictionary+=ROS2
% LTeX: dictionary+=YARP


\title{Programming Robots with higher-Order Reactive programming}
\pretitle{\flushleft{Graduation thesis submitted in partial fulfilment of the
requirements for the degree of de
Ingenieurswetenschappen: Computerwetenschappen}}
\author{Gérard Lichtert}
\date{2025-2026}
\promotors{Promotor: Prof.\ Dr.\ Wolfgang De Meuter\\
Advisor: Bjarno Oeyen}
\faculty{Sciences and Bio-engineering Sciences} % Note: without the word "Faculty"!

\begin{document}
\frontmatter
\maketitle%

% Oftentimes, you need add a second language title.
\title{Robots programmeren met hogere-orde reactief programmeren}
\pretitle{\flushleft{Proefschrift ingediend met het oog op het behalen van de graad van Master of Science in de Ingenieurswetenschappen: Computerwetenschappen}}%
\date{2025-2026}%
\faculty{Wetenschappen en Bio-ingenieurswetenschappen}%
\maketitle%

\raggedright
% LTeX: enabled=true
\chapter*{Abstract}

\textbf{Reactive programming} (RP) is a declarative programming paradigm based on
data streams and the propagation of change. In a nutshell, if we have two
variables x and y and another variable z that sums both x and y, it should be sufficient to
update either x or y and z should be updated automatically.
\par
There are several approaches to achieve reactive programming. One approach is to
create a dedicated language. In this case the reactivity will be native to the
language. Another option is to extend a language with reactive features, like
%example

\tableofcontents

\mainmatter

% LTeX: dictionary+=ReactiveX
\chapter{Introduction}
Modern software is often written in such a way that it is event-driven, or in other
words, reactive. This means that applications reacts to an incoming event
by creating subsequent events that arise as a reaction to the incoming event.
Such an application is often called a reactive system. Reactive systems are
often implemented with callbacks or by implementing the Observer Pattern.
However, this does not come without its drawbacks as this may lead to
\enquote{inversion of control} or \enquote{callback
	hell}\cite{surveyRP2024, bainomugisha2013survey}. To alleviate this we
introduce \textbf{Reactive Programming}. A programming paradigm that allows the
user to write reactive systems in a declarative way. Traditionally the sum and
the product of
two elements would be written as shown in Listing~\ref{lst:imperative-product-sum}.
\par
\begin{listing}[htbp]
	\begin{lstlisting}[language=C]
int x = 5;
int y = 10;
int s = x + y;
int p = x * y;
// x changes state
x = 10;
// s requires recomputation
s = x + y;
// p requires recomputation
p = x * y;
    \end{lstlisting}
	\caption{Imperative product and sum}
	\label{lst:imperative-product-sum}
\end{listing}
Whenever $x$ or $y$ would change state, you would have to manually update
$s$ and $p$. Reactive programming allows you to declare $x$ and $y$ as sources of data
and $s$ and $p$ as the result of the relevant computations.
Whenever the value of $x$ or $y$ would change, the variables
dependent on the sources would be automatically recomputed, and the result
propagated further in the reactive system. Schematically the dependency graph of product-sum would look
like Figure~\ref{fig:dependency-graph-product-sum}
\begin{figure}[H]
	\centering
	\includegraphics[scale=0.7]{./images/product-sum-graph.drawio.pdf}
	\caption{Dependency graph for product-sum}
	\label{fig:dependency-graph-product-sum}
\end{figure}
There are different ways of how such a reactive system could be created using
reactive programming. One way is to use a programming which itself is natively
reactive. A few examples are Haai~\cite{oeyen2024reactive},  Another is to add native reactivity to an existing language by
extending it the language or by implementing a reactive library such as ReactiveX.
We will be looking at the former and more specifically the higher-order reactive
programming language \textbf{Haai}, which was developed at the SOFT lab at the VUB.
Haai uses reactors as their
As a preview, an example of how such a product and sum could be written in Haai is shown
in Listing~\ref{lst:reactive-product-um}. Haai uses reactors as its native way
of declaring a reactive program.
\begin{listing}[htbpp]
	\begin{lstlisting}[language=haai]
(defr (product-sum x y)
    (def sum (+ x y))
    (def product (* x y))
    (out product sum))
    \end{lstlisting}
	\caption{Product sum in Haai}
	\label{lst:reactive-product-um}
\end{listing}\\
Additionally, we want to delve into the world of electronics.
From maintaining your fridge's temperature to controlling a drones'
altitude, electronics are deeply embedded in everyone's day-to-day life. Writing
software for these electronics can also be challenging. This mainly due to the fact that
using peripherals require using their associated libraries, which are not always
standardized. Another challenge is that creating programs for microcontrollers
often require the developer to use a low-level programming language such as C/C++ or
Rust, which come with their own challenges. Nowadays, there are some efforts
being made to provide a standardized collection of tools to aid with the development of
systems with many peripherals, or robots. There is Robot Operating Systems
(ROS)~\cite{allaban2021developing}, Open Robot Control Software (OROCOS)~\cite{bruyninckx2001open}, Yet Another Robot
Platform (YARP)~\cite{metta2006yarp} to name a few.
We will be focussing on using \textbf{(ROS)}.
\section{Contributions}
Reactive programming is used across a multitude of domains. It is used in web
and mobile development for instance. However, we will be focussing on using it
for the Internet of Things (IoT). IoT consists of multiple physical objects,
usually with very limited computing power and/or memory. In this thesis we
introduce a way of declaring a reactive program across multiple devices from a
single source. This means that the developer declares the program once, along
with declarations where which part of the program lives and then deploys the
program to the relevant devices making use of ROS.
This is achieved by the following components:
\begin{itemize}

	\item An extension of the Remus VM~\cite{oeyen2024virtual}, more
	      specifically, extending the language to accept a declaration of where
	      which part of the program lives. The Remus compiler will use this
	      metadata to split the Haai reactor declarations into an embedded part
	      and a server part. Each piece will then be available for the user to
	      be deployed in their respective parts of the IoT network.
	\item A new tool called CHaai, which translates the Remus bytecode
	      obtained from compiling the Haai program into a C program. The C
	      program will be required to run the program on microcontrollers using ROS.
	\item A C port of the existing Remus VM written in rust~\cite{descheemaeker2023compilation}, also created to allow developers to create reactive
	      programs for microcontrollers. However, it is adapted with respects to
	      the current Instruction Set Architecture (ISA) of the Racket version
	      of Remus~\cite{oeyen2024virtual} and allows usage of ROS.
\end{itemize}
\section{Overview of this Dissertation}
Chapter 2 gives the problem statement as well as an overview of the current
reactive programming solutions and existing toolkits to program networks of
microcontrollers. It also introduces a concept called \enquote{Tierless
	programming}, which will inspire the declarative way of telling Remus where
which part of the reactive program lives.
\par
Chapter 3 introduces $\mu$-Remus. A C port of the Remus VM, which was previously
written in Rust as well as in Racket. It explains why the C port was necessary
as well as conceptualizes the link between the ROS communication protocols and
the dependency graph generated by reactive programming.
\par
Chapter 4 demonstrates usage of the Haai language extensions inspired by
Tierless programming to declare where which part of the reactive program lives.
It will also tell you about the CHaai tool. Which will create a C version of the
reactive program. It is ported to C to be compatible with ROS.
\par
Chapter 5 will evaluate the tools and extensions driven by an example. The
target microcontroller will be a Raspberry Pi Pico H (2021) and will be used
along a PC.
\par
Lastly, chapter 6 will conclude this dissertation and give notes on some
possible future work.
\section{Approach}
We first start by discussing the state of the art, what tools already exist and
then move on to the problem statement and continue with the contributions part
of the dissertation. However, since it is heavily dependent on a language
and a toolkit we will first give a brief introduction into some relevant parts
of the language or toolkit, each time providing the necessary context and information, prior to moving on to
the actual contributions.
\chapter{State of the Art}
Different programming languages have adopted various paradigms, including
functional or logic models. Yet imperative programming remains a popular
paradigm due to how popular languages have been designed. However, as applications shifted towards
event-driven architectures, traditional imperative programming did not come
without its drawbacks. Traditional imperative programming was simple. Each
imperative program was code that defined its step-by-step control flow and state
transitions. However, when an imperative programming language is used for an
event-driven architecture, some problems may arise, such as \enquote{inversion of
	control} or \enquote{callback hell}. Inversion of control is a problem
commonly associated with codebases using frameworks, where instead of the
programmer calling functions provided by the framework, the framework calls the functions
defined by the programmer.
\par
\begin{listing}
	\begin{lstlisting}[language=JavaScript]
func1(argument0, function(argument1){
    // compute result1
    func2(result1, function(argument2){
        // compute result2
        func3(result2, function(argument3){
            // compute result3
            func4(result3, function(argument4){
                //etc..
            })
        })
    })
})\end{lstlisting}
	\caption{Illustration of callback hell in JavaScript}
	\label{lst:illustration-of-callback-hell-in-javascript}
\end{listing}
Callback hell occurs when programmers are required to
pass a callback function as an argument to another function, which calls the passed
function with the result of its computation, yet that function also expects a
function to be passed to continue its execution. While this is not necessary an issue if
the nesting is one or two layers deep, it becomes \enquote{hell} once it goes
multiple layers deep or ad infinitum, as shown in
Listing~\ref{lst:illustration-of-callback-hell-in-javascript}. Reactive
programming seeks to solve these issues.
\section{Reactive Programming}
\label{sec:reactive-programming}
Reactive programming (RP) is a declarative programming paradigm that allows the
developer to declare a program in terms of dependencies, instead of having to
manually manage these dependencies. The actual propagation of the state change
in the dependency chain is abstracted away in the language design. For instance,
in a program you could have two variables $x$ and $y$ that hold
values and variables $s$ and $p$ which hold the sum and product of $x$ and $y$
respectively.
\begin{listing}
	\begin{lstlisting}[language=C]
int x = 5;
int y = 10;
int s = x + y;
printf("%d", s) // 15;
x = 10; // state change
printf("%d", s) // 15;
// requires manual recomputation of s
s = x + y;
printf("%d", s) // 20;
\end{lstlisting}
	\caption{Manual recomputation of $s$, a dependent on $x$}
	\label{lst:manual-recomputation-of-s-a-dependent-on-x}
\end{listing}
Under the imperative paradigm, whenever $x$ or $y$ would change
state, it would require manual recomputation of $s$ as shown in
Listing~\ref{lst:manual-recomputation-of-s-a-dependent-on-x}.
In reactive programming, it is sufficient to update either $x$ or $y$ and the change is
propagated throughout its system automatically, as shown in
Listing~\ref{lst:automatic-recomputation-of-s-a-dependent-on-x}.
\begin{listing}
	\begin{lstlisting}[language=C]
int x = 5;
int y = 10;
int s = x + y;
printf("%d", s) // 15;
x = 10; // state change
// No manual recomputation needed
printf("%d", s) // 20;
\end{lstlisting}
	\caption{Automatic recomputation of $s$, a dependent on $x$ (Pseudocode)}
	\label{lst:automatic-recomputation-of-s-a-dependent-on-x}
\end{listing}
In essence, it reacts to changes in its input values and automatically propagates it
through itself and updates its output values. These values are also called
\textit{signals}.
There seem to be two main families in how reactive programming is implemented.
One of which is functional reactive programming (FRP) and the other graph-based
reactive programming. FRP languages such as E-FRP, FRPNow or Yampa are often implemented in a host language,
such as, but not limited to: Haskell, Racket, Java, Scala or JavaScript~\cite{surveyRP2024}. These FRP
languages then inherit the toolchains of the host language. However, there are
also FRP languages are that are an independent language. They do not
rely on a host language but instead have their own dedicated tools (compiler,
interpreter, etc\dots). FRP Languages consist of lambdas or functions that are
(re)-applied every turn in the reactive system.
\par
\begin{figure}[H]
	\centering
	\includegraphics{./images/sum-dag.drawio.pdf}
	\caption{DAG for Listing~\ref{lst:automatic-recomputation-of-s-a-dependent-on-x}}
	\label{fig:dag-for-automatic-sum}
\end{figure}
Graph-based reactive programming languages compile the program into a directed
acyclic graph (DAG), which is also known as the dependency graph of the program.
The DAG describes the data flow between different components within the program,
as an example, we can construct the DAG of
Listing~\ref{lst:automatic-recomputation-of-s-a-dependent-on-x}. Since $s$ depends
on the result of the sum, and the sum depends on $x$ and $y$, we can construct the
DAG as shown in Figure~\ref{fig:dag-for-automatic-sum}. Here we can see that $x$
and $y$ flow to the sum, whose result flows to $s$. Examples of graph-based
reactive programming languages are REScala, FrTime and Flapjax.
\par
A reactive application created with an RP language built on top of a host
language usually consists of two parts. A reactive part of code, which is a
\enquote{reactive} extension of the host language and a non-reactive part of the code.
The non-reactive part of the code are features that are made available from the
host language through a mechanism known as
\textit{lifting}~\cite{oeyen2024reactive}. However, lifting can lead to issues
such a diverging code, which leads to unresponsive programs or a mismatch
between the reactive and non-reactive code, which leads to faulty behaviour. The
SOFT lab at the VUB proposes a novel reactive programming language called
\textbf{Haai}~\cite{oeyen2024reactive} (Pronounced \enquote{high}) that is
meticulously designed to be reactive-only, avoiding these issues completely.
\par
\begin{listing}
	\begin{lstlisting}[language=haai]
(defr (sum-product x y)
    (def s (+ x y))
    (def p (* x y))
    (out s p))\end{lstlisting}
	\caption{Reactive program in Haai}
	\label{lst:reactive-program-in-haai}
\end{listing}
Haai uses reactors as its only computational abstraction for declaring a
reactive program. Which is a declarative way of defining signals and their
dependencies. A Haai program consists of reactor definitions (defined using
$defr$) and reactor deployments (similar to application expressions in Racket).
An example of such a Haai program can be seen in
Listing~\ref{lst:reactive-program-in-haai}. Here we can see that the reactor
reacts to the input signals $x$ and $y$, and computes their sum and product
by deploying the $*$ and $+$ reactor with the input signals and outputs the
result of the deployments. Upon changing the values of the input signals, the reactor will
automatically recompute their dependant output values.
\par
\section{RP languages targeting MCUs}
\label{sec:rp-languages-targeting-mcus}
In Section~\ref{sec:reactive-programming} we discussed reactive programming.
Reactive programming has many applications, from web applications that react to
the input of users to embedded devices, which react to signals from hardware.
In this thesis we want to focus on RP related to embedded devices, whose usage
has increased due to their wide variety of applications, such as on the internet
of things (IoT), edge computing and artificial intelligence (AI)~\cite{shi2016edge, gubbi2013internet, liu2023space}.
\par
As previously mentioned, many RP languages are built on top of host languages
which often require the toolchains of those host languages to run. However, in
environments where resources are constrained such as on microcontrollers, it is
hard to ship an RP program created in Yampa. This is due to the fact that Haskell
runtimes are very large and do not fit the resource constraints on
microcontrollers. Despite this challenge there have been efforts to get RP
onto microcontrollers.
\par
\begin{figure}[h]
	\centering
	\includegraphics[scale=0.9]{./images/emfrp-compilation-diagram.drawio.pdf}
	\caption{EmFRP development workflow~\cite{sawada2016emfrp}}
	\label{fig:emfrp-development-workflow}
\end{figure}
An example would be \textbf{EmFRP}, an FRP language
created by K. Sawada and T. Watanabe that mainly targets small-scale embedded
systems~\cite{sawada2016emfrp}. Like Haai, EmFRP is a graph-based RP
language. This means that EmFRP follows the same philosophy in terms of values
that depend on other signals and automatic change propagation. More concretely, a
EmFRP program is written in terms of nodes, who may refer to other nodes that it
depends on. Conceptually it works the same as the DAG that Haai generated in
Figure~\ref{fig:dag-for-automatic-sum}.
\par
However, since EmFRP is designed to work on small-scale embedded
systems, some design considerations have been made. For instance, the dependency
graph must be known statically, meaning it cannot change during runtime.
Additionally, it does not allow recursion in function or in type definitions and
nodes must always have a name. This because they are not first class values in the
language.
\par
\begin{listing}
	\begin{lstlisting}[language=emfrp]
module Thermostat
in
    temperature : Int,  # temp sensor
    pulse1min : Bool    # periodical pulse
out
    switching : Bool    # heater state
use
    Std                 # standard library

node init [ False ] switching =
    if pulse1min then
        temperature < 50
    else
        switching@last  # previous value\end{lstlisting}
	\caption{Thermostat controller in EmFRP~\cite{sawada2016emfrp}}
	\label{lst:thermostat-controller-emfrp}
\end{listing}
\begin{listing}
	\begin{lstlisting}[language=c]
#include "Thermostat.h"
void Input(int *temperature, int *pulse1min){
    /* Your code goes here ... */
}

void Output(int *switching){
    /* Your code goes here ... */
}

int main(){
    ActivateThermostat();
}\end{lstlisting}
	\caption{I/O Code Skeleton for Thermostat~\cite{sawada2016emfrp}}
	\label{lst:io-code-skeleton-for-thermostat}
\end{listing}
Furthermore, interacting with hardware requires using magic constants
such as memory addresses, that are architecture- or peripheral specific. To
address this, the EmFRP compiler generates an I/O code skeleton to connect the
program with the hardware. The developer remains responsible for completing the
skeleton code though.
\par
The way EmFRP works is as follows: The developer creates a series of EmFRP
modules, the EmFRP compiler then generates I/O skeleton code in C and FRP code
in C. The developer is then required to fill in the I/O skeleton code. A
schematic of the workflow can be seen in
Figure~\ref{fig:emfrp-development-workflow}.
\par
In the paper introducing EmFRP, they show an example with a thermostat, where
each minute, the temperature is read and depending on the temperature value, the
heater is turned on or off. The code for this example can be seen in
Listing~\ref{lst:thermostat-controller-emfrp}. The EmFRP program is then
compiled into the I/O skeleton shown in
Listing~\ref{lst:io-code-skeleton-for-thermostat} as well as the FRP code in C.
Once the skeleton code is completed, the program can be further compiled by the
relevant C compilers into the required binaries.
\par
Another RP language targeting embedded devices is CFRP~\cite{suzuki_cfrp_embedded}. By the same creators of
EmFRP, K. Sawada, T. Watanabe, as well as K.Suzuki and K. Nagayama, CFRP was
created as a pure FRP language that compiles to stand-alone C++ code. It was
created to show that FRP is beneficial for developing software for small-scale
embedded devices.
\par
\begin{listing}
	\begin{lstlisting}[language=cfrp]
 -- Including C++ header files
 %{
 #include "sensors.h"
 #include "ac_ctrl.h"
 %}

 -- Declaring identifiers connected to external devices
 %input tmp :: Signal Float = sen_tmp;
 %input hmd :: Signal Float = sen_hmd;
 %import ac :: Signal Bool -> Signal () = ctrl_ac;

 -- Main expression
 let di t h = t - 0.55 * (1 - 0.01 * h) * (t - 14.5) in
 ac (lift1 (\x -> x > 24) (lift2 di tmp hmd))\end{lstlisting}
	\caption{Example program in CFRP~\cite{suzuki_cfrp_embedded}}
	\label{lst:example-program-in-cfrp}
\end{listing}
It was designed as follows: A CFRP program consists of a series of declarations,
which denote signals and links to hardware (in C++). Followed by a single
expression that builds to reactive system. An example can be seen in
Listing~\ref{lst:example-program-in-cfrp} where the air conditioner is turned on
or off depending on a discomfort index. The discomfort index is calculated from
the temperature (tmp) and humidity (hmd). Notably it seems to use the same
\enquote{base} datatypes that C++ provides but signals get an additional
annotation (the Singal type). This is to make the datatype predictable at compile time.
\par
Furthermore, it uses a single Haskell-like expression to construct a dependency
graph, just like EmFRP and Haai do. Which is then used to compile to the C++
program. The caveat with CFRP is that C++ objects and functions referred to in
the CFRP file have to be declared in separate C++ files. This means that only
the \enquote{global} reactive system is declared in CFRP and the rest in C++.
\par
Whilst there are more RP languages targeting embedded systems like
Atom~\cite{hawkins3008controlling} or Copilot~\cite{pike2010copilot}. They seem
all to compile to a low-level programming language such as C, C++, Rust or
Zig. Though the more customary languages that these RP languages compile to are either
C or C++.
\par
At the VUB a virtual machine (VM) was created for Haai to be able to run Haai on the bare metal.
The VM is called Remus~\cite{oeyen2024virtual}, which was later re-implemented
in Rust and with some modifications a prototype was created to run a Haai
program on a M5Stick~\cite{descheemaeker2023compilation}. The benefit of
maintaining a scheme/racket-like syntax with dynamic typing remains, at the cost
of having to compile the Haai program to Remus bytecode. Which can then be
interpreted by the Rust based implementation of Remus.
\par
At this point one could ask him or herself what the point is, of examining these
RP languages that target MCUs. Well as mentioned in
Section~\ref{sec:rp-languages-targeting-mcus}, due to the increase in use of
microcontrollers. Though in this thesis we want to focus more on a system of
these embedded devices. Take a robot for example: A robot has sensors as input
and has other peripherals that are then used to interact with the real world,
depending on these inputs. In other words, the robot reacts
to the sensors by instructing the motors what to do or where to move next.
\par
To build this system of embedded devices we could create some reactive programs
in an RP language of choice that targets these devices, but this would leave the
developer responsible for implementing all the communication between the devices
as well as some central computing unit that coordinates all these devices and
peripherals. Most of the time requiring the developer to use a library that uses
some kind of communication protocol under the hood (Bluetooth, UART, I2C, SPI,
USB, etc\dots). Easier would be if we could use some kind of standardized toolkit
that saves the developer from these complexities, which is what we will be looking
into next.
\section{Systems to program robots}

In this section we are going to have a look at the following systems that aid
development in robotics:
Open Robots Control Software (OROCOS)~\cite{bruyninckx2001open}, Robot Operating
System (ROS)~\cite{quigley2009ros, macenski2022robot, allaban2021developing, belsare2023microros} and Yet
Another Robot Platform (YARP)~\cite{metta2006yarp}.
\begin{figure}[htbp]
	\centering
	\begin{subfigure}[b]{0.20\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./images/orocos-logo.jpeg}
		\caption{OROCOS logo}
		\label{fig:orocos-logo}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.25\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./images/ros-logo.pdf}
		\caption{ROS logo}
		\label{fig:ros-logo}
	\end{subfigure}
	\hfill
	\begin{subfigure}[b]{0.20\textwidth}
		\centering
		\includegraphics[width=\textwidth]{./images/yarp-logo.png}
		\caption{YARP logo}
		\label{fig:yarp-logo}
	\end{subfigure}

	\caption{Robotics platforms}
	\label{fig:robotics-platforms}
\end{figure}
\subsection{OROCOS}
OROCOS, started at the Katholieke Universiteit
Leuven (KUL), as a project aimed at becoming a general-purpose and open robot
control software package. On a more technical side of things, it is software for
all sorts of robotic devices and computer platforms (operating systems). OROCOS
features software components for kinematics, dynamics, planning, sensing,
control, interfacing, etc\dots~through a middleware. The system also provides
features in the form of modules that can be split up in the following
categories:
\begin{itemize}
	\item Supporting modules
	      \par
	      Software that does not interact with the hardware directly but
	      rather supports the interaction with it. These modules can range from
	      simple mathematical modules to aid vector operations, configuration
	      modules, simulation modules to real-time operating system
	      modules.
	\item Robotics modules
	      These are the modules specifically interacting with the hardware such as
	      kinematics and dynamics modules or servo controller modules. These
	      robotics modules tend to use one or more of the supporting modules to
	      make the usage or development of these robotics modules easier.
	\item Components
	      These are the Common Object Request Broker Architecture (CORBA) with
	      their Interface Definition Language (IDL) descriptions. These components
	      are built upon the previous modules and are used as the building blocks
	      of robots, as they allow for multi-machine inter-process
	      communication.
\end{itemize}
Unfortunately OROCOS does not allow it to be used on the bare metal. Meaning it
is only usable on a system that has an OS. However, with the first version of
ROS, the real-time toolchain (RTT) of OROCOS, called OROCOS RTT was often used alongside ROS.
Nowadays, the second version of ROS, or ROS2 is used in favour of ROS + OROCOS
RTT, or the OROCOS Component Library (OCL) as ROS2 absorbed both the toolchain
and component features of OROCOS and thus no longer needs them as middleware.
The hardware libraries for Kinematics Dynamics (KDL) or Bayesian Filtering (BFL)
are still used. OROCOS also provides support to interop with other robotics
systems such as ROS and YARP.
\subsection{ROS}
Originating from Stanford, ROS provides a structured communication layer above
the operating systems of a compute cluster~\cite{quigley2009ros}. A ROS cluster
consists of several on-board devices and off-board devices, which can connect
and disconnect at runtime. Consequently, the designers of ROS chose for a
peer-to-peer (p2p) system as having a central server would mean a lot more network
saturation as messages would have to a central point and would then have to
be forwarded to their respective recipients. In a p2p system each message would
be forwarded to an intermediary recipient who has the address of the final
recipient or another intermediary, leading to less network saturation and
shorter distances travelled.
\par
The philosophy of ROS of using multiple standalone components within its system
allows for developers to reuse or repurpose existing hardware libraries within
ROS, but without actually depending on ROS. This also allows for robust testing
of these libraries, without being dependent on ROS. The decoupling of these
components also means that they can be used in other robotics systems where
there is no inherent dependency and vice versa, giving ROS access to existing
standalone components, such as drivers and vision algorithms.
\par
As previously mentioned, the designers of ROS chose for a p2p system. However,
the actual implementation is divided into nodes, messages, topics and services.
Nodes are processes that perform computations. This means that each device in
the system could consist of one or more nodes. Since these nodes also
communicate with each other, we could create a graph with the edges denoting
where the nodes send their messages to.
\par
\begin{figure}[h]
	\centering
	\includegraphics[width=0.7\textwidth]{./images/ROS-nodes-example.drawio.pdf}
	\caption{ROS node graph example}
	\label{fig:ros-node-graph-example}
\end{figure}
Figure~\ref{fig:ros-node-graph-example} shows a
robotics example where some products are being transported down a conveyor belt.
However, these products have to be classified by colour, which the sensors on the
\enquote{sensors} device can detect. The sensors can then send the detected
colour to the operator computer, which selects the appropriate sorter to sort the
product. Each sorter consists of an arm and a sensor. The sensor detects when
the product is in the sorting range and sends a signal to the arm, which sorts
it. Furthermore, the operator computer shows the detected colours on
the screen and allows the user to manually operate a sorter if need be thus
completing the graph shown in the figure.
\par
\begin{figure}
	\centering
	\includegraphics[width=0.7\textwidth]{./images/ROS-nodes-example-improved1.drawio.pdf}
	\caption{ROS node graph with topics}
	\label{fig:ros-nodes-example-improved}
\end{figure}
Messages that these nodes send to each other are actually strictly typed
datastructures. These can consist of the primitive types (integer, floating
point, boolean, etc\dots), arrays of primitive types as well as constants.
Messages can also be composed of other messages or arrays of messages which can
be nested arbitrarily deep.
To be able to broadcast messages to multiple devices, ROS sends messages through
a topic. Nodes subscribe to a particular topic if they wish to receive
messages published to the topic.
This allows for a many-to-many relationship
within the ROS node graph. A re-illustration of
Figure~\ref{fig:ros-node-graph-example} can be seen in
Figure~\ref{fig:ros-nodes-example-improved}, this time
illustrated with the topics, where unidirectional one-to-one, one-to-many,
many-to-many or many-to-one relationships are allowed. A bidirectional
relationship between two nodes can be created by
making one of the nodes a subscriber to a topic and the other a publisher as
well as the inverse on another topic as seen on the operator computer.
\par
Whilst topics are the main communication protocol that ROS uses, it is lacking for
synchronous communication. To solve this, the designers implemented services,
which works analogous to the web services. A node places a request to a uniquely
named node which returns a response after processing the request, just like web
services.
\begin{figure}[H]
	\centering
	\includegraphics[width=\linewidth]{./images/haai-product-sum-in-ros.drawio.pdf} % Replace with your image file
	\caption{Product sum in a ROS node system}
	\label{fig:haai-product-sum-to-ros}
\end{figure}
\par
Interestingly, we can use these communication protocols to create a
graph-like structure, just like the dependency graphs that are generated from a
PR program in Haai, EmFRP or CFRP. Schematically we can recreate the DAG defined
in Figure~\ref{fig:dependency-graph-product-sum} in a ROS system, as shown in
Figure~\ref{fig:haai-product-sum-to-ros}. lorem ipsum ipsum ipsum ipsum ipsum






\subsection{YARP}



\section{Tierless Programming}
\section{Problem Statement}
\chapter{$\mu$-Remus}
\section{Haai \& Remus}
\section{ROS \& $\mu$-ROS}
\section{Mapping RP to ROS \& $\mu$-ROS}
\section{$\mu$-Remus: A C implementation}
\chapter{Programming Robots with Reactors}
\section{Linking ROS and $\mu$-ROS}
\chapter{Tierless Haai}
\section{Haai to CHaai}
\chapter{Evaluation}
\section{Evaluating Haai distribution}
\section{Evaluating Haai to CHaai}
\section{Evaluating CHaai on $\mu$-Remus}
\chapter{Conclusion \& Future Work}
\backmatter%







\printbibliography%
\end{document}
